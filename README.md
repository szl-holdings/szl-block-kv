# szl-block-kv

Canonical GitHub source for `SZLHOLDINGS/szl-block-kv`.

```python
from szl_block_kv import PagedCache, paged_attn, selfcheck
print(selfcheck())
```

Not a vLLM rehost. GPU paged Triton is ROADMAP (v0 is a correct torch gather).
Λ = Conjecture 1. Apache-2.0.
